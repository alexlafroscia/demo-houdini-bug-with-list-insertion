### This file was generated by Nexus Schema
### Do not make changes to this file directly

type Item implements Node {
	id: ID!
	message: String!
}

type List implements Node {
	id: ID!
	items: [Item!]!
}

type Mutation {
	addItem(listID: ID!, message: String!): Item!
}

interface Node {
	id: ID!
}

type Query {
	list(id: ID!): List
	lists: [List!]!
	node(id: ID!): Node
}
